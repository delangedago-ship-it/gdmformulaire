# Mot de passe pour accéder à /admin
ADMIN_PASSWORD=change-me

# Ces variables sont créées automatiquement par Vercel quand vous ajoutez
# les intégrations "Redis" (KV) et "Blob" au projet — vous n'avez normalement
# rien à saisir vous-même en production.
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_URL=
BLOB_READ_WRITE_TOKEN=
